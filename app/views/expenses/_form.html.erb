<%= form_with(model: expense, url: user_category_expenses_path(current_user, @category)) do |form| %>
  <% if expense.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
        <% expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <di>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </div>

  <%# <div>
    <%# form.label :user_id, style: "display: block" %>
    <%# form.text_field :user_id, value: current_user.id %>
  
  <div class="form-group custom-dropdown">
  <label for="category-select" class="control-label">Select Category</label>
  <div class="options">
<%= select_tag 'expense[category_ids]', options_for_select(@categories.map { |g| [g.name, g.id] }, @expense.category_ids), id: 'category_select', class: "form-control", multiple: true %>
  </div>
</div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
